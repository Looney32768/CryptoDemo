springdoc:
  api-docs.path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method

datasource:
  url: "jdbc:relique:csv:classpath:data/prices"

server:
  error:
    whitelabel:
      enabled: false

spring:
  main:
    banner-mode: off